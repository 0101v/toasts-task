import React from "react";
import { ToastWrapper } from './component';

export const Toast = () => {
  const [toastArr, setToastArr] = useState();

  return (
    <ToastWrapper>
      
    </ToastWrapper>
  )
}